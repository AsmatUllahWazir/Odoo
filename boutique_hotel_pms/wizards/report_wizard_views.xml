<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Report Wizard Form View -->
    <record id="view_hotel_report_wizard_form" model="ir.ui.view">
        <field name="name">hotel.report.wizard.form</field>
        <field name="model">hotel.report.wizard</field>
        <field name="arch" type="xml">
            <form string="Generate Report">
                <sheet>
                    <div class="oe_title">
                        <h1>Generate Report</h1>
                    </div>
                    <group>
                        <group>
                            <field name="report_type"/>
                            <field name="date_from"/>
                            <field name="date_to"/>
                            <field name="format"/>
                        </group>
                        <group>
                            <field name="room_type_id"
                                   invisible="report_type not in ['occupancy','revenue']"/>
                            <field name="partner_id"
                                   invisible="report_type not in ['guest','reservations']"/>
                            <field name="state"
                                   invisible="report_type != 'reservations'"/>
                        </group>
                    </group>
                    <footer>
                        <button name="action_generate_report"
                                string="Generate Report"
                                type="object"
                                class="btn-primary"/>
                        <button string="Cancel"
                                class="btn-secondary"
                                special="cancel"/>
                    </footer>
                </sheet>
            </form>
        </field>
    </record>
</odoo>