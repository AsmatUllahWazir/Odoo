<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_picking_form_barcode_scanner" model="ir.ui.view">
        <field name="name">stock.picking.form.barcode.scanner</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_open_barcode_scanner"
                        type="object"
                        string="Scan Barcodes"
                        class="btn-primary"
                        invisible="state in ('done', 'cancel')"/>
            </xpath>
        </field>
    </record>

    <record id="view_picking_tree_barcode_scanner" model="ir.ui.view">
        <field name="name">stock.picking.tree.barcode.scanner</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_internal_search"/>
        <field name="arch" type="xml">
            <xpath expr="//search" position="inside">
                <filter name="filter_barcode_scanner_ready"
                        string="Ready for Scanning"
                        domain="[('state', 'in', ('assigned', 'confirmed', 'waiting'))]"/>
            </xpath>
        </field>
    </record>
</odoo>